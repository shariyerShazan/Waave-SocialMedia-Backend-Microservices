name: Media Service CI/CD

on:
  push:
    branches:
      - main
      - develop
    paths:
      - ".github/workflows/media-service.yml"
      - ".github/workflows/_reusable-service-cicd.yml"
      - "apps/media-service/**"
      - "libs/common/**"
      - "libs/kafka/**"
      - "libs/proto-schema/**"
      - "libs/grpc-clients/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig*.json"
  pull_request:
    branches:
      - main
      - develop
    paths:
      - ".github/workflows/media-service.yml"
      - "apps/media-service/**"
      - "libs/common/**"
      - "libs/kafka/**"
      - "libs/proto-schema/**"
      - "libs/grpc-clients/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig*.json"
  workflow_dispatch: {}

# jobs:
#   ci-cd:
#     uses: ./.github/workflows/_reusable-service-cicd.yml
#     with:
#       service_name: media-service
#       nest_project: media-service
#       k8s_deployment: media-service
#       ecr_repository: waave-media-service

#     secrets:
#       AWS_GITHUB_ACTIONS_ROLE_ARN: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
#       AWS_REGION: ${{ vars.AWS_REGION }}
#       EKS_CLUSTER_NAME: ${{ vars.EKS_CLUSTER_NAME }}
