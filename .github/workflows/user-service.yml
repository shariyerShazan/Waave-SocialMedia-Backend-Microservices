name: User Service CI/CD

on:
  push:
    branches:
      - main
      - develop
    paths:
      - ".github/workflows/user-service.yml"
      - ".github/workflows/_reusable-service-cicd.yml"
      - "apps/user-service/**"
      - "libs/common/**"
      - "libs/kafka/**"
      - "libs/proto-schema/**"
      - "libs/grpc-clients/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig*.json"
  pull_request:
    branches:
      - main
      - develop
    paths:
      - ".github/workflows/user-service.yml"
      - "apps/user-service/**"
      - "libs/common/**"
      - "libs/kafka/**"
      - "libs/proto-schema/**"
      - "libs/grpc-clients/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig*.json"
  workflow_dispatch: {}

# jobs:
#   ci-cd:
#     uses: ./.github/workflows/_reusable-service-cicd.yml
#     with:
#       service_name: user-service
#       nest_project: user-service
#       k8s_deployment: user-service
#       ecr_repository: waave-user-service
#       prisma_schema_path: "apps/user-service/prisma/schema.prisma"
#     secrets:
#       AWS_GITHUB_ACTIONS_ROLE_ARN: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
#       AWS_REGION: ${{ vars.AWS_REGION }}
#       EKS_CLUSTER_NAME: ${{ vars.EKS_CLUSTER_NAME }}
