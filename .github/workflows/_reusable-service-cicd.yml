# name: Reusable Service CI/CD

# on:
#   workflow_call:
#     inputs:
#       service_name:
#         description: "Folder name under apps/, e.g. post-service"
#         required: true
#         type: string
#       nest_project:
#         description: "Nest project name to build (usually same as service_name)"
#         required: true
#         type: string
#       k8s_deployment:
#         description: "Kubernetes Deployment + container name to update"
#         required: true
#         type: string
#       ecr_repository:
#         description: "ECR repository name (usually waave-<service_name>)"
#         required: true
#         type: string
#       prisma_schema_path:
#         description: "Path to prisma schema, or empty if this service has no database"
#         required: false
#         type: string
#         default: ""
#     secrets:
#       AWS_GITHUB_ACTIONS_ROLE_ARN:
#         required: true
#       AWS_REGION:
#         required: true
#       EKS_CLUSTER_NAME:
#         required: true

# permissions:
#   id-token: write   # required to mint the OIDC token for aws-actions/configure-aws-credentials
#   contents: read

# jobs:
#   build-and-test:
#     name: Build & Unit Test
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm

#       - name: Install Dependencies
#         run: npm ci

#       - name: Generate Prisma Client
#         if: inputs.prisma_schema_path != ''
#         run: npx prisma generate --schema=${{ inputs.prisma_schema_path }}

#       - name: Build ${{ inputs.service_name }}
#         run: npx nest build ${{ inputs.nest_project }}

#   docker-build-push:
#     name: Build & Push Image to ECR
#     needs: build-and-test
#     if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
#     runs-on: ubuntu-latest
#     outputs:
#       image_uri: ${{ steps.image.outputs.uri }}
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Configure AWS Credentials (OIDC)
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: Log in to Amazon ECR
#         id: ecr-login
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Build and Push Docker Image
#         id: image
#         env:
#           REGISTRY: ${{ steps.ecr-login.outputs.registry }}
#           REPOSITORY: ${{ inputs.ecr_repository }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker buildx build \
#             --file apps/${{ inputs.service_name }}/Dockerfile \
#             --tag "$REGISTRY/$REPOSITORY:$IMAGE_TAG" \
#             --tag "$REGISTRY/$REPOSITORY:latest" \
#             --cache-from type=gha \
#             --cache-to type=gha,mode=max \
#             --push \
#             .
#           echo "uri=$REGISTRY/$REPOSITORY:$IMAGE_TAG" >> "$GITHUB_OUTPUT"

#   deploy:
#     name: Deploy to EKS
#     needs: docker-build-push
#     if: |
#       (github.event_name == 'push' || github.event_name == 'workflow_dispatch') &&
#       (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
#     runs-on: ubuntu-latest
#     environment: ${{ github.ref == 'refs/heads/main' && 'production' || 'staging' }}
#     steps:
#       - name: Configure AWS Credentials (OIDC)
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: Update kubeconfig
#         run: |
#           aws eks update-kubeconfig \
#             --name "${{ secrets.EKS_CLUSTER_NAME }}" \
#             --region "${{ secrets.AWS_REGION }}"

#       - name: Roll out new image
#         run: |
#           kubectl set image "deployment/${{ inputs.k8s_deployment }}" \
#             "${{ inputs.k8s_deployment }}=${{ needs.docker-build-push.outputs.image_uri }}" \
#             -n waave

#       - name: Wait for rollout
#         run: |
#           kubectl rollout status "deployment/${{ inputs.k8s_deployment }}" \
#             -n waave --timeout=300s
